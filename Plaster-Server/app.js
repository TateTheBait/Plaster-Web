const express = require('express');
const app = express();
const port = 2150;

app.get('/connect', (req, res) => {
  
});


app.listen(port, () => {
  console.log(`Example app listening at http://localhost:${port}`);
});

while (true) {
    // keep server running
    task.sleep(1000);
}

while (true) {//testing hackatype currently.
}